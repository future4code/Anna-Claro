/*  a - undefined
    b - null
    c - 11
    d - 3
    e - [ 3, 19, 5, 6, 7, 8, 9, 10, 11, 12, 13 ]
    f - 9
    SUBI NUM ONIBUS EM MIRROCOS. 28 */

    // Exercicios de escrita

    /*const nome = prompt("Qual é o seu nome?")
    const email = prompt("Qual é o seu e-mail?")
    const frase = `O e-mail ${email} foi cadastrado com sucesso. Seja bem-vinda(o), ${nome}!`
    console.log(frase)

    const comidas = ["lasanha", "pizza", "strogonoff", "brownie", "feijoada"]
    console.log(comidas)
    const comidasFrase = `Essas são as minhas comidas preferidas: ${comidas}`

    const comidaPreferida = prompt("Qual a sua comida preferida?")
    comidas.splice(1,1, comidaPreferida)
    console.log(comidas)

    
    const tarefa1 = prompt("Primeira tarefa do dia:")
    const tarefa2 = prompt("Segunda tarefa do dia:")
    const tarefa3 = prompt("Terceira tarefa do dia:")
    const listaDeTarefas = []

    listaDeTarefas.push(tarefa1)
    listaDeTarefas.push(tarefa2)
    listaDeTarefas.push(tarefa3)
    console.log(listaDeTarefas)

    const tarefaRealizada = Number(prompt('Digite uma tarefa 1-3')) -1
    listaDeTarefas.splice(tarefaRealizada, 1)
    console.log(listaDeTarefas)*/